{
  "name": "turn-beep",
  "description": "Focus-aware turn-end feedback for Claude Code: sound always, plus a Windows toast (shown as Warp) only when Warp is not the foreground window. Fully configurable via /turn-beep. Windows only.",
  "version": "0.4.0",
  "author": { "name": "Rivay.Park" }
}
